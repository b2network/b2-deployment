kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: b2-state-db-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: b2-state-db-pv
  storageClassName: nfs-csi
